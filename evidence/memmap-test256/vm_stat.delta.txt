# Captured on 26.2 (25C56), Apple M4 Max
# Xcode 26.2 (17C52)
# Produced by: vm_stat before/at-peak, differenced
#

system-wide vm_stat delta, baseline -> peak (pad 256 MB):
  anonymous (dirty, swappable)               +2773 MB
  file-backed (clean, evictable)               +23 MB
  wired                                       +766 MB
  compressor                                    -1 MB

If the growth were the kernel caching a mapped binary, it would
appear as file-backed pages, which are clean and evictable under
pressure. Anonymous growth is a private, dirty copy: it cannot be
dropped, only compressed or swapped.
